<h1> list of all the groups available: </h1>

  <h3>groups available</h3>
  <% if current_user %>
		<%= link_to 'create new group', new_group_path%>
	<% else %>
	 <p><%= 'register to create groups!' %></p>
	 <%end %>

	<ul>
 <% @groups.each do |group| %>
 	<li><%= link_to group.name, group_path(group)%></li>
 <% end %>
 </ul>
