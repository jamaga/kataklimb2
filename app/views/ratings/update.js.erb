$('#star').raty({
  readOnly: true,
  score: <%= @place.average_rating %>,
  path: '/assets'
});

$('#place_star').raty({
  score: <%= @rating.score %>,
  path: '/assets',
  click: function(score, evt) {
    $.ajax({
      url: '/ratings/' + <%= @rating.id %>,
      type: 'PATCH',
      data: { score: score }
    }).done(function (data){});
  }
});