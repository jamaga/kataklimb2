# Description

my project called Puja el peu, webpage for creating climbing groups in Catalunya

```
$ rbenv local 2.1.3
$ bundle install
$ heroku config  --app dashing-threescale > .env
```
# Heroku

```
$ rbenv local 2.1.3
$ bundle install
$ heroku config  --app dashing-threescale > .env
```
